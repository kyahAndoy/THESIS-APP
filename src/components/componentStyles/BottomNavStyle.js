import { makeStyles } from '@mui/styles';

const useStyle = makeStyles({
    root: {
    width: '100%',
    position: 'fixed',
    bottom: 0,
    left: 0,
    borderTop: '#78909C',
  },
});

export default useStyle;